* Reader/Writer
- Turtle
* Graph implementations
- URI graph
* SPARQL query implementation
* Move the Ruby query execution into the Query class
* Reasoner and rules
* New (streaming) HTTP library for Sesame graph
* RDF/XML parse type literal parsing needs some help
* Mock out the HTTP requests in the Sesame specs
* Ruby 1.9 support
* Do I need to include a call to super in subclasses of Graph?
* Performance improvements on ActiveRecord graph
